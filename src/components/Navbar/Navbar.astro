---
import Image from "astro/components/Image.astro";
import chevron from "#assets/icons/chevron.png";
import logo from "#assets/icons/logo.svg";
import Navlink, { type NavLinkProps } from "./Navlink.astro";

const links: NavLinkProps[] = [
  { title: "About", href: "/about" },
  { title: "Services", href: "/services" },
  { title: "People", href: "/people" },
  { title: "Insights", href: "/insights" },
  { title: "Contacts", href: "/contacts" },
];
---

<nav
  class="w-screen z-max flex justify-between items-center fixed top-0 left-0 h-20 px-5"
>
  <a href="/"><Image src={logo} alt="personal logo" class="w-44" /></a>

  <ul class="gap-5 hidden sm:flex">
    {links.map((l) => <Navlink title={l.title} href={l.href} />)}
  </ul>
</nav>
